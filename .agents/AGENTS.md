# Siyantra Workspace Rules

- **Git Commits & Pushes**: Do NOT stage (`git add`), commit (`git commit`), or push (`git push`) code changes automatically. Only execute git version control commands when the user explicitly asks you to do so.
